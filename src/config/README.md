# src/config

Global configuration.

Use this folder for stable app configuration:

- routes;
- navigation;
- public page registry;
- global SEO defaults;
- global option maps.

Rules:

- Keep data small and stable.
- Do not put page copy here; use `src/content`.
- Do not put database records here once a domain has moved to Prisma.
