# src/styles

Global styles and design tokens.

Use this folder for:

- reset and document base styles;
- design tokens;
- shared utilities.

Rules:

- Prefer component-level Tailwind classes for local layout.
- Keep global CSS intentional and stable.
- Do not add feature-specific CSS here unless it is truly reusable.
